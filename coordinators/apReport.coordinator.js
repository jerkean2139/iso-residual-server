import ApReportModel from '../models/apReport.model.js';

export default class ApReportCoordinator {

    /* example 
    static getUser = async (userID) => await ApReportModel.getUser(userID);
    */
}